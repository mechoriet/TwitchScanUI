import { environment } from "../../environments/environment";

export const version = environment.version;
export const backendUrl = environment.backendUrl;
export const twitchEmbedUrl = environment.twitchEmbedUrl;
export const frontendUrl = environment.frontendUrl;